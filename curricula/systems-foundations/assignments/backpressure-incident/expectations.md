A strong response distinguishes transport buffering, application queueing,
worker service time, and scheduler delay. It uses distributions and rates rather
than median latency alone. Admission is bounded at a named point, overload is
observable to clients, retries are controlled, and recovery avoids a fresh
surge. Fairness, utilization, latency, and dropped work are treated as explicit
trade-offs rather than hidden consequences.
