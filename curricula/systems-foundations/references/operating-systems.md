# Operating-system reasoning

Separate virtual abstractions from physical effects. State which behavior is an
API guarantee and which is an implementation tendency. When diagnosing latency,
connect observed events to scheduling, faults, descriptors, or contention.
